{
  "name": "nbd-pro-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore + Storage rules unit tests for NBD Pro. Run against the Firebase emulator.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project nobigdeal-pro-test 'node ./firestore-rules.test.js'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.0.0"
  }
}
